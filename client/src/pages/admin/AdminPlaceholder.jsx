export default function AdminPlaceholder({ title }) {
  return (
    <div>
      <h1 className="text-2xl font-bold sm:text-3xl">{title}</h1>
      <p className="mt-2 text-muted">This section is coming in the next step.</p>
    </div>
  )
}